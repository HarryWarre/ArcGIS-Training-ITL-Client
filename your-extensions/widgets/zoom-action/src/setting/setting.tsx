import { React } from "jimu-core";
import { AllWidgetSettingProps } from "jimu-for-builder";

const Setting = (props: AllWidgetSettingProps<any>) => {
  return <p>This is Setting site</p>;
};

export default Setting;
